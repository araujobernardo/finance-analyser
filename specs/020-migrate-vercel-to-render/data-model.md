# Data Model: Migrate Deployment from Vercel to Render

No new entities, tables, or schema changes are introduced by this feature.

This is a deployment platform migration. All existing data models, database schema, and API contracts remain unchanged.

See `src/db/schema.ts` for the current schema (accounts, transactions, users, assets, liabilities).
